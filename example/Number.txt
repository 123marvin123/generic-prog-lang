namespace number {
    //concept Number (description: "number") : Object;
    concept Natural (description: "natural number, possibly zero") : number::Number & ordered::Ordered;
    concept Cardinal (description: "cardinal number") : number::Number & ordered::Ordered;
    concept Integer (description: "integer number") : number::Number & ordered::Ordered;
    concept Rational (description: "rational number") : number::Number & ordered::Ordered;
    //concept Real (description: "real number") : number::Number;
    concept Complex (description: "complex number") : number::Number& ordered::Ordered;

    fun succ(p1: <T>) -> T {
        description { "successor of p(1)" }
    }

    fun pred(p1: <T>) -> T {
        description { "predecessor of p(1)" }
    }

    fun pow(p1: <T>, p2: <K>) -> T {
        description { "p(2)-th power of p(1)" }
    }
}
